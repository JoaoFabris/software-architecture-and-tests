const passengerModel = require('./passenger.model');
const passengerByID = require('./passenger.model');
const travelModel = require('./travel.model');

module.exports = {
  passengerModel,
  passengerByID,
  travelModel,
};
